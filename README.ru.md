# ru-smb-pd-anonymizer 🔒

[![CI](https://github.com/ranas-mukminov/ru-smb-pd-anonymizer/actions/workflows/ci.yml/badge.svg)](https://github.com/ranas-mukminov/ru-smb-pd-anonymizer/actions/workflows/ci.yml)
[![Security](https://github.com/ranas-mukminov/ru-smb-pd-anonymizer/actions/workflows/security.yml/badge.svg)](https://github.com/ranas-mukminov/ru-smb-pd-anonymizer/actions/workflows/security.yml)
[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](LICENSE)
[![Python Version](https://img.shields.io/badge/python-3.10%2B-blue)](https://www.python.org/downloads/)

🇷🇺 Русская версия | 🇬🇧 [English version](README.md)

---

## Описание

**ru-smb-pd-anonymizer** — это production-ready Python-фреймворк для анонимизации и псевдонимизации российских персональных данных перед передачей датасетов в AI/LLM-сервисы, аутсорсинговые команды, аналитические отделы или службы технической поддержки. Разработан специально для малого и среднего бизнеса в России и предоставляет инструменты маскирования, токенизации и преобразования данных с учётом требований российского законодательства о защите персональных данных (152-ФЗ).

Фреймворк включает детекторы для российских идентификаторов (ФИО, паспорта, ИНН, СНИЛС, телефоны, адреса, email), переиспользуемые трансформеры для различных стратегий анонимизации, готовые политики для типовых сценариев и адаптеры для популярных инструментов обработки данных: pandas, SQLAlchemy, Apache Airflow и dbt. Опциональный AI-слой помогает формировать политики анонимизации на основе анализа схемы, не отправляя реальные персональные данные наружу по умолчанию.

Это технический инструмент: юридическая оценка достаточности анонимизации остаётся на стороне ваших юристов или службы комплаенса.

---

## Основные возможности

- **Детекторы российских ПДн** – Автоматическое распознавание ФИО, паспортов, ИНН, СНИЛС, телефонов, почтовых адресов и email с помощью регулярных выражений и эвристик
- **Гибкие стратегии трансформации** – Маскирование, токенизация, хеширование с солью, обобщение, добавление шума, интерфейсы для Format-Preserving Encryption (FPE)
- **Готовые политики** – Предустановленные правила анонимизации для аналитики, обучения ML, техподдержки и обработки логов
- **Поддержка нескольких форматов** – Обработка CSV-файлов, pandas DataFrames, SQL-запросов через SQLAlchemy, интеграция с Airflow DAG и dbt-макросами
- **CLI и Python API** – Интерфейс командной строки для быстрых задач и программный API для интеграции в существующие пайплайны
- **AI-подсказки для политик** – Опциональный анализ схемы данных и рекомендации политик без раскрытия реальных персональных данных
- **Ориентация на комплаенс** – Разработан с учётом требований российского законодательства, поддержка как необратимой анонимизации, так и обратимой псевдонимизации
- **Production-grade инструментарий** – Типобезопасный код с mypy, полный набор тестов, CI/CD-пайплайны, проверки безопасности с bandit и pip-audit
- **Open source** – Лицензия Apache 2.0 для коммерческого и некоммерческого использования

---

## Архитектура и компоненты

Фреймворк состоит из нескольких модульных компонентов, работающих совместно:

```
┌─────────────────────────────────────────────────────────────┐
│                      CLI / Python API                        │
│       (команды ru-pd-anon, импорты библиотеки Python)        │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   v
┌─────────────────────────────────────────────────────────────┐
│                   Адаптеры пайплайнов                        │
│  • pandas адаптер (CSV, DataFrames)                          │
│  • SQLAlchemy адаптер (запросы к БД)                         │
│  • Airflow оператор (задачи DAG)                             │
│  • dbt макросы (SQL-трансформации)                           │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   v
┌─────────────────────────────────────────────────────────────┐
│           Движок политик + Классификация                     │
│  • Детекция полей (семантические типы: ФИО, Телефон и т.д.) │
│  • Оценка рисков (уровни чувствительности)                   │
│  • Применение политик (правила на основе YAML)               │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   v
┌─────────────────────────────────────────────────────────────┐
│                   Слой трансформеров                         │
│  • Маскирование/токенизация ФИО                              │
│  • Маскирование паспортов                                    │
│  • Токенизация телефонов/email                               │
│  • Хеширование ИНН/СНИЛС                                     │
│  • Трансформеры обычных ID                                   │
│  • Обобщение адресов                                         │
└─────────────────────────────────────────────────────────────┘
```

**Поток данных:**
1. Исходные данные поступают через CLI, Python API или адаптер пайплайна (pandas, SQLAlchemy, Airflow, dbt)
2. Схема анализируется, поля классифицируются по семантическим типам
3. Движок политик применяет соответствующие правила на основе типов полей и уровней чувствительности
4. Трансформеры выполняют анонимизацию/псевдонимизацию для подходящих полей
5. Анонимизированные данные записываются в выходное назначение

---

## Требования

### Системные требования

- **Операционная система:** Linux (Ubuntu 20.04+, Debian 11+, RHEL 8+, Rocky Linux 8+), macOS 11+, или Windows 10+ с WSL2
- **Python:** 3.10 или выше
- **CPU/Память:** Минимум – 1 ядро CPU, 512 МБ ОЗУ для базового использования; рекомендуется 2+ ядра и 2+ ГБ ОЗУ для больших датасетов
- **Дисковое пространство:** ~100 МБ для пакета и зависимостей

### Зависимости Python

Основные зависимости (устанавливаются автоматически):
- `pandas>=1.5` – Обработка DataFrame
- `pydantic>=2.6` – Валидация схем и типобезопасность
- `PyYAML>=6.0` – Парсинг файлов политик
- `typer>=0.9` – CLI-интерфейс
- `sqlalchemy>=1.4` – Поддержка запросов к БД
- `rich>=13.0` – Форматирование вывода в терминал

### Опциональные зависимости

Для разработки и тестирования:
```bash
pip install ru-smb-pd-anonymizer[dev,test]
```

Для интеграции с Apache Airflow:
- `apache-airflow>=2.5` (не включён в пакет, устанавливается отдельно)

Для интеграции с dbt:
- `dbt-core>=1.5` (не включён в пакет, устанавливается отдельно)

### Требования к доступу

- **Установка Python-пакетов:** Доступ в Интернет к PyPI или локальному зеркалу
- **Файловая система:** Права на чтение/запись для каталогов входных/выходных данных
- **Доступ к БД:** Соответствующие учётные данные при использовании SQLAlchemy-адаптера

---

## Быстрый старт

Начните работу менее чем за 5 минут:

**1. Установите пакет**
```bash
pip install ru-smb-pd-anonymizer
```

**2. Проанализируйте схему ваших данных**
```bash
ru-pd-anon inspect-schema \
  --input examples/data/synthetic_crm.csv \
  --format csv \
  --out schema.json
```

**3. Сгенерируйте политику анонимизации**
```bash
ru-pd-anon suggest-policy \
  --schema schema.json \
  --use-case analytics \
  --out policy.yaml
```

**4. Примените политику для анонимизации данных**
```bash
ru-pd-anon apply-policy \
  --input examples/data/synthetic_crm.csv \
  --format csv \
  --schema schema.json \
  --policy policy.yaml \
  --output examples/data/synthetic_crm_anon.csv
```

Хотите быстро показать ценность продукту для продаж или комплаенса? Добавьте флаг `--report`, чтобы получить краткий отчёт о том,
сколько строк обработано и какие столбцы были анонимизированы:

```bash
ru-pd-anon apply-policy \
  --input examples/data/synthetic_crm.csv \
  --format csv \
  --schema schema.json \
  --policy policy.yaml \
  --output examples/data/synthetic_crm_anon.csv \
  --report
```

**5. Проверьте результаты**
```bash
head examples/data/synthetic_crm_anon.csv
```

Вы должны увидеть замаскированные ФИО, токенизированные номера телефонов и email, а также захешированные идентификаторы, при этом структура данных сохраняется для аналитики.

---

## Подробная установка

### Установка из PyPI (рекомендуется)

```bash
# Создайте виртуальное окружение (рекомендуется)
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или на Windows: venv\Scripts\activate

# Установите пакет
pip install ru-smb-pd-anonymizer

# Проверьте установку
ru-pd-anon --version
```

### Установка из исходников

```bash
# Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-smb-pd-anonymizer.git
cd ru-smb-pd-anonymizer

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите в режиме разработки с dev-зависимостями
pip install -e .[dev,test]

# Запустите тесты для проверки установки
pytest

# Запустите линтер и проверку типов
scripts/lint.sh
```

### Установка с Docker (опционально)

```bash
# Клонируйте репозиторий
git clone https://github.com/ranas-mukminov/ru-smb-pd-anonymizer.git
cd ru-smb-pd-anonymizer

# Соберите Docker-образ
docker build -t ru-smb-pd-anonymizer:latest .

# Запустите анонимизацию в контейнере
docker run --rm \
  -v $(pwd)/examples/data:/data \
  ru-smb-pd-anonymizer:latest \
  ru-pd-anon apply-policy \
    --input /data/synthetic_crm.csv \
    --format csv \
    --policy /data/policy_analytics.yaml \
    --output /data/synthetic_crm_anon.csv
```

---

## Настройка

### Файлы политик

Политики анонимизации определяются в формате YAML. Политика задаёт:
- Паттерны для сопоставления полей (по имени или семантическому типу)
- Уровень анонимизации (необратимая, односторонняя псевдонимизация, обратимая псевдонимизация)
- Применяемый трансформер
- Опциональные параметры для трансформера

**Пример политики для аналитики:**

```yaml
# examples/configs/policy_analytics.yaml
name: example_analytics
use_case: analytics
description: "Сценарий аналитики с агрессивной анонимизацией"

fields:
  # ФИО: необратимое маскирование
  - field_pattern: "semantic:FIO"
    level: ANONYMIZED_IRREVERSIBLE
    transformer: fio.FioMaskingTransformer
  
  # Паспорта: частичное маскирование
  - field_pattern: "semantic:Passport"
    level: PSEUDONYMIZED_ONE_WAY
    transformer: passport.PassportMaskingTransformer
    params:
      keep: 2  # Оставить первые 2 цифры
  
  # Телефоны: токенизация (обратима при наличии ключа)
  - field_pattern: "semantic:Phone"
    level: PSEUDONYMIZED_REVERSIBLE
    transformer: phone.PhoneTokenizationTransformer
  
  # Email: токенизация
  - field_pattern: "semantic:Email"
    level: PSEUDONYMIZED_REVERSIBLE
    transformer: email.EmailTokenizationTransformer
  
  # Внутренние ID: односторонний хеш
  - field_pattern: "semantic:CustomID"
    level: PSEUDONYMIZED_ONE_WAY
    transformer: generic_id.GenericIdHashingTransformer

global_rules:
  require_irreversible_for_sensitive: true
```

**Пример политики для обучения ML:**

```yaml
# examples/configs/policy_ml_training.yaml
name: ml_training
use_case: ml
description: "ML-обучение с балансом приватности и полезности данных"

fields:
  - field_pattern: "semantic:FIO"
    level: ANONYMIZED_IRREVERSIBLE
    transformer: fio.FioGeneralizationTransformer  # Оставить первую букву
  
  - field_pattern: "semantic:Phone"
    level: PSEUDONYMIZED_ONE_WAY
    transformer: phone.PhoneHashingTransformer  # Консистентный хеш
  
  - field_pattern: "semantic:Email"
    level: PSEUDONYMIZED_ONE_WAY
    transformer: email.EmailDomainOnlyTransformer  # Оставить только домен
  
  - field_pattern: "semantic:INN"
    level: PSEUDONYMIZED_ONE_WAY
    transformer: inn.InnHashingTransformer

global_rules:
  preserve_nulls: true
  preserve_types: true
```

### Переменные окружения

Опциональная настройка через переменные окружения:

```bash
# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
export RU_PD_ANON_LOG_LEVEL=INFO

# Пользовательский каталог политик
export RU_PD_ANON_POLICY_DIR=/etc/ru-pd-anon/policies

# Эндпоинт AI-сервиса (при использовании внешнего AI для подсказок политик)
export RU_PD_ANON_AI_ENDPOINT=http://localhost:8000

# Отключить AI-функции (по умолчанию: включены)
export RU_PD_ANON_DISABLE_AI=true

# Пользовательская соль для хеширующих трансформеров
export RU_PD_ANON_HASH_SALT=<ваша-безопасная-случайная-соль>
```

### Файлы схем

Файлы схем описывают структуру данных и генерируются автоматически при выполнении `inspect-schema`:

```json
{
  "columns": [
    {
      "name": "customer_id",
      "dtype": "int64",
      "semantic_type": "CustomID",
      "nullable": false
    },
    {
      "name": "full_name",
      "dtype": "string",
      "semantic_type": "FIO",
      "nullable": false
    },
    {
      "name": "phone",
      "dtype": "string",
      "semantic_type": "Phone",
      "nullable": true
    }
  ]
}
```

---

## Использование и типовые задачи

### Интерфейс командной строки

**Анализ схемы данных и обнаружение полей с ПДн:**
```bash
ru-pd-anon inspect-schema \
  --input data/customers.csv \
  --format csv \
  --out customers_schema.json
```

**Генерация политики анонимизации на основе схемы:**
```bash
ru-pd-anon suggest-policy \
  --schema customers_schema.json \
  --use-case analytics \
  --out policy_customers.yaml
```

**Применение политики для анонимизации данных:**
```bash
ru-pd-anon apply-policy \
  --input data/customers.csv \
  --format csv \
  --schema customers_schema.json \
  --policy policy_customers.yaml \
  --output data/customers_anon.csv
```

**Валидация синтаксиса файла политики:**
```bash
ru-pd-anon validate-policy --policy policy_customers.yaml
```

### Python API

**Анонимизация pandas DataFrame:**

```python
import pandas as pd
from ru_smb_pd_anonymizer.policies.model import Policy
from ru_smb_pd_anonymizer.pipeline.pandas_adapter import anonymize_dataframe

# Загрузите ваши данные
df = pd.read_csv("data/customers.csv")

# Загрузите политику анонимизации
policy = Policy.from_yaml("policy_analytics.yaml")

# Анонимизируйте
df_anon = anonymize_dataframe(df, policy)

# Сохраните результат
df_anon.to_csv("data/customers_anon.csv", index=False)
```

**Прямая анонимизация CSV-файлов:**

```python
from ru_smb_pd_anonymizer.policies.model import Policy
from ru_smb_pd_anonymizer.pipeline.pandas_adapter import anonymize_csv

policy = Policy.from_yaml("policy_analytics.yaml")
anonymize_csv("data/customers.csv", "data/customers_anon.csv", policy)
```

**Использование с SQLAlchemy:**

```python
from sqlalchemy import create_engine
from ru_smb_pd_anonymizer.policies.model import Policy
from ru_smb_pd_anonymizer.pipeline.sqlalchemy_adapter import anonymize_query

engine = create_engine("postgresql://user:pass@localhost/db")
policy = Policy.from_yaml("policy_analytics.yaml")

query = "SELECT * FROM customers WHERE created_at > '2024-01-01'"
result_df = anonymize_query(engine, query, policy)
```

### Интеграция с Apache Airflow

**Создание DAG анонимизации:**

```python
from datetime import datetime
from airflow import DAG
from ru_smb_pd_anonymizer.pipeline.airflow_operator import RuSmbPdAnonymizerOperator

with DAG(
    dag_id="anonymize_crm_daily",
    start_date=datetime(2024, 1, 1),
    schedule_interval="@daily",
    catchup=False,
) as dag:
    anonymize_task = RuSmbPdAnonymizerOperator(
        task_id="anonymize_crm",
        input_path="/data/crm_{{ ds }}.csv",
        output_path="/data/crm_anon_{{ ds }}.csv",
        policy_path="/config/policy_analytics.yaml",
        input_format="csv",
    )
```

### Интеграция с dbt

**Использование макроса анонимизации в dbt-модели:**

```sql
-- models/customers_anon.sql
{{
  config(
    materialized='table'
  )
}}

SELECT
  customer_id,
  {{ ru_pd_anon_mask_fio('full_name') }} as full_name,
  {{ ru_pd_anon_tokenize_phone('phone') }} as phone,
  {{ ru_pd_anon_tokenize_email('email') }} as email,
  created_at
FROM {{ ref('customers_raw') }}
```

---

## Обновление

### Обновление пакета из PyPI

```bash
# Активируйте ваше виртуальное окружение
source venv/bin/activate

# Обновите до последней версии
pip install --upgrade ru-smb-pd-anonymizer

# Проверьте новую версию
ru-pd-anon --version
```

### Обновление из исходников

```bash
cd ru-smb-pd-anonymizer
git pull origin main

# Переустановите с последними изменениями
pip install -e .[dev,test]

# Запустите тесты для проверки
pytest
```

### Критические изменения

При обновлении между мажорными версиями просмотрите [CHANGELOG.md](CHANGELOG.md) на предмет критических изменений:

- **Изменения формата политик:** YAML-схема политик может измениться; валидируйте ваши политики после обновления: `ru-pd-anon validate-policy --policy your_policy.yaml`
- **Изменения API трансформеров:** Пользовательские трансформеры могут требовать обновления при изменении базовых классов
- **Изменения аргументов CLI:** Проверьте справку CLI после обновления: `ru-pd-anon --help`

После обновления всегда валидируйте ваши политики и тестируйте на образце данных перед использованием в продакшене.

---

## Логи, мониторинг и устранение неполадок

### Логи

**Логирование CLI:**
```bash
# Включить debug-логирование
export RU_PD_ANON_LOG_LEVEL=DEBUG
ru-pd-anon apply-policy --input data.csv --policy policy.yaml --output data_anon.csv
```

**Программное логирование:**
```python
import logging
logging.basicConfig(level=logging.DEBUG)

from ru_smb_pd_anonymizer.pipeline.pandas_adapter import anonymize_csv
# Детальные логи будут выведены
```

### Типичные проблемы и решения

**Проблема: `FileNotFoundError: policy.yaml not found`**
```bash
# Решение: Используйте абсолютные пути или проверьте текущий каталог
ru-pd-anon apply-policy --policy $(pwd)/policy_analytics.yaml ...
```

**Проблема: `No transformers applied, output identical to input`**
```bash
# Решение: Проверьте детекцию схемы и паттерны полей в политике
# 1. Проанализируйте схему
ru-pd-anon inspect-schema --input data.csv --format csv --out schema.json
# 2. Проверьте определённые семантические типы в schema.json
cat schema.json
# 3. Убедитесь, что field_pattern политики соответствует семантическим типам
```

**Проблема: `ImportError: No module named 'ru_smb_pd_anonymizer'`**
```bash
# Решение: Проверьте установку и виртуальное окружение
pip list | grep ru-smb-pd-anonymizer
# Если не найден, переустановите
pip install ru-smb-pd-anonymizer
```

**Проблема: `TypeError: expected str, got None`**
- **Причина:** Null-значения в столбцах, помеченных для анонимизации
- **Решение:** Установите `preserve_nulls: true` в global_rules политики

**Проблема: Airflow-оператор падает с ошибками импорта модулей**
```bash
# Решение: Установите пакет в окружении Airflow
# В docker-compose.yml или Dockerfile Airflow:
pip install ru-smb-pd-anonymizer
# Или добавьте в requirements.txt, используемый Airflow
```

**Проблема: `PermissionError` при записи выходных данных**
```bash
# Решение: Проверьте права на запись
chmod u+w output_directory/
```

**Проблема: Падение производительности на больших датасетах**
- **Причина:** Обработка всего DataFrame в памяти
- **Решение:** Используйте чанковую обработку или SQLAlchemy-адаптер для потоковой передачи

**Проблема: Значения хешей непостоянны между запусками**
- **Причина:** Отсутствующее или изменяющееся значение соли
- **Решение:** Установите постоянную соль через переменную окружения:
```bash
export RU_PD_ANON_HASH_SALT=<фиксированное-безопасное-значение>
```

---

## Замечания по безопасности

> [!WARNING]
> Этот инструментарий выполняет техническую анонимизацию/псевдонимизацию. Юридическая квалификация того, являются ли выходные данные «персональными данными» по российскому или международному праву, должна быть оценена отдельно вашими юристами.

### Рекомендации по безопасности

1. **Не логируйте чувствительные данные:**
   - Избегайте вывода реальных ПДн в логи или в терминал
   - Никогда не коммитьте файлы с реальными персональными данными в систему контроля версий
   - Используйте синтетические или предварительно анонимизированные примеры в тестах и документации

2. **Защищайте файлы политик и соли:**
   - Храните файлы политик в безопасных местах с соответствующими правами доступа к файлам
   - Используйте сильные случайные значения соли для хеширующих трансформеров
   - Периодически меняйте соли при использовании обратимой псевдонимизации
   - Храните значения солей в системах управления секретами (например, HashiCorp Vault, AWS Secrets Manager)

3. **Защищайте ключи токенизации:**
   - При использовании обратимой токенизации защищайте ключи шифрования с той же строгостью, что и сами персональные данные
   - Используйте аппаратные модули безопасности (HSM) или сервисы управления ключами (KMS) для продакшена

4. **Сетевая безопасность:**
   - При использовании функций AI-подсказок с внешними сервисами используйте HTTPS и проверяйте TLS-сертификаты
   - Рассмотрите возможность использования локальных AI-моделей, чтобы избежать отправки информации о схеме наружу

5. **Контроль доступа:**
   - Ограничьте доступ к процессам анонимизации только уполномоченному персоналу
   - Логируйте операции анонимизации для аудиторских следов
   - Реализуйте ролевой контроль доступа (RBAC) для управления политиками

6. **Минимизация данных:**
   - Анонимизируйте только те данные, которые действительно нужно передавать
   - Используйте наиболее строгий уровень анонимизации, который всё ещё удовлетворяет требованиям вашего сценария
   - Предпочитайте необратимую анонимизацию (маскирование) псевдонимизации, когда это возможно

7. **Проверка и тестирование:**
   - Вручную проверяйте образцы анонимизированных выходных данных перед передачей
   - Тестируйте политики на репрезентативных датасетах
   - Периодически проводите оценку риска реидентификации

8. **Соответствие законодательству:**
   - Убедитесь, что методы анонимизации соответствуют нормативным требованиям (152-ФЗ, 233-ФЗ)
   - Документируйте ваши процессы анонимизации и оценки рисков
   - Консультируйтесь с уполномоченными по защите данных (DPO) и юристами

> [!CAUTION]
> **Псевдонимизация — это не анонимизация.** Обратимые трансформации (токенизация, хеширование с известной солью) всё ещё представляют собой обработку персональных данных по российскому законодательству. Используйте необратимое маскирование или обобщение для истинной анонимизации, где необходимо предотвратить реидентификацию.

---

## Структура проекта

```
ru-smb-pd-anonymizer/
├── src/ru_smb_pd_anonymizer/       # Исходный код основного пакета
│   ├── ai/                          # AI-анализ схем и подсказки политик
│   ├── classification/              # Детекция полей с ПДн и оценка рисков
│   ├── cli/                         # Интерфейс командной строки
│   ├── dtypes/                      # Определения типов данных и схем
│   ├── io/                          # Адаптеры ввода/вывода (CSV, JSON, БД)
│   ├── pipeline/                    # Адаптеры пайплайнов (pandas, SQLAlchemy, Airflow, dbt)
│   ├── policies/                    # Модели политик и встроенные политики
│   └── transforms/                  # Трансформеры (маскирование, токенизация, хеш и т.д.)
├── examples/                        # Примеры датасетов, политик и интеграций
│   ├── configs/                     # Примеры YAML-файлов политик
│   ├── data/                        # Синтетические датасеты для тестирования
│   ├── airflow/                     # Примеры Airflow DAG
│   ├── dbt/                         # dbt-макросы и модели
│   └── notebooks/                   # Jupyter notebooks с примерами использования
├── tests/                           # Юнит- и интеграционные тесты
│   ├── unit/                        # Юнит-тесты для отдельных компонентов
│   └── integration/                 # End-to-end интеграционные тесты
├── scripts/                         # Скрипты разработки и обслуживания
│   ├── lint.sh                      # Линтинг кода (ruff, mypy)
│   ├── security_scan.sh             # Проверки безопасности (bandit, pip-audit, safety)
│   └── perf_check.sh                # Бенчмарки производительности
├── pyproject.toml                   # Метаданные пакета и зависимости
├── CHANGELOG.md                     # История версий и изменений
├── CONTRIBUTING.md                  # Руководство по внесению вклада
├── LICENSE                          # Лицензия Apache 2.0
├── LEGAL.md                         # Юридические оговорки и заметки о комплаенсе
└── README.md                        # Этот файл
```

---

## Планы развития

Будущие улучшения и возможности:

- [ ] Поддержка дополнительных российских типов ПДн (ОГРН, КПП, автомобильные номера)
- [ ] Механизмы дифференциальной приватности для агрегированной аналитики
- [ ] Интеграция со Spark и Dask для крупномасштабной распределённой обработки
- [ ] Веб-интерфейс для управления политиками и визуализации
- [ ] Предобученные ML-модели для улучшенной детекции семантических полей
- [ ] Поддержка неструктурированных данных (текстовые документы, emails)
- [ ] Расширенные реализации FPE (Format-Preserving Encryption)
- [ ] Kubernetes-оператор для пакетных задач анонимизации
- [ ] Интеграция логов аудита (Elasticsearch, Splunk)

См. [открытые issues](https://github.com/ranas-mukminov/ru-smb-pd-anonymizer/issues) для запросов сообщества и отчётов об ошибках.

---

## Как внести вклад

Мы приветствуем вклад сообщества! Чтобы внести свой вклад:

1. **Откройте issue**, описывающий ваш сценарий использования, предлагаемую функцию или отчёт об ошибке
   - Предоставьте контекст: объём данных, требования комплаенса, окружение
   - **Никогда не включайте реальные персональные данные** в issues, pull requests или примеры кода

2. **Отправьте pull request**
   - Сохраняйте изменения небольшими и сфокусированными на одной функции или исправлении
   - Добавьте юнит-тесты для новых трансформеров или функций
   - Обновите документацию (README, docstrings, CHANGELOG)
   - Запустите линтеры и тесты перед отправкой: `scripts/lint.sh && pytest`

3. **Настройка для разработки:**
   ```bash
   git clone https://github.com/ranas-mukminov/ru-smb-pd-anonymizer.git
   cd ru-smb-pd-anonymizer
   python3 -m venv venv
   source venv/bin/activate
   pip install -e .[dev,test]
   ```

4. **Стиль кода:**
   - Используйте type hints для всех функций
   - Следуйте PEP 8 и PEP 257
   - Сохраняйте функции небольшими и тестируемыми
   - Никогда не логируйте и не выводите реальные персональные данные в тестах, debug-выводе или примерах

5. **Тестирование:**
   - Запуск тестов: `pytest`
   - Проверка покрытия: `pytest --cov=ru_smb_pd_anonymizer`
   - Проверка безопасности: `scripts/security_scan.sh`

6. **Юридическая оговорка:**
   - Этот проект — технический инструмент; юридическая оценка достаточности анонимизации остаётся на стороне юристов
   - Все контрибьюторы соглашаются с условиями лицензии Apache 2.0

См. [CONTRIBUTING.md](CONTRIBUTING.md) для подробных руководств.

---

## Лицензия

Этот проект распространяется под лицензией **Apache License 2.0**.

Вы можете свободно использовать, модифицировать и распространять это программное обеспечение в коммерческих и некоммерческих целях при условии соблюдения условий лицензии.

См. файл [LICENSE](LICENSE) для полного текста лицензии.

---

## Автор и коммерческая поддержка

**Автор:** [Ranas Mukminov](https://github.com/ranas-mukminov)

Проект разрабатывается и поддерживается DevOps- и DevSecOps-инженером, специализирующимся на безопасных пайплайнах данных и автоматизации комплаенса для российских компаний.

### Коммерческая поддержка

Для внедрения в продакшене, разработки пользовательских стратегий анонимизации, аудита комплаенса или постоянной поддержки доступны коммерческие услуги:

- **Аудит инфраструктуры и проектирование пайплайнов анонимизации** – Оценка текущих потоков данных и проектирование workflow-процессов анонимизации, соответствующих требованиям
- **Интеграция с существующими системами** – Внедрение анонимизации в ваши платформы Airflow, dbt, Spark или пользовательские платформы данных
- **Консалтинг по комплаенсу** – Навигация по требованиям российского законодательства о защите данных (152-ФЗ, 233-ФЗ) с применением технических мер
- **Разработка пользовательских трансформеров** – Создание специализированных трансформеров анонимизации под ваши бизнес-потребности
- **Обучение и поддержка** – Обучение на месте или удалённо для ваших инженерных команд и команд работы с данными

**Контакт:** Посетите [https://run-as-daemon.ru](https://run-as-daemon.ru) или свяжитесь через [профиль GitHub](https://github.com/ranas-mukminov).

---

**Оговорка:** Этот фреймворк является техническим инструментом и не гарантирует юридической достаточности анонимизации. Ответственность за законную обработку, хранение, трансграничную передачу и локализацию персональных данных остаётся на контролёре данных. Всегда консультируйтесь с квалифицированными юристами для оценки комплаенса.
